;; Originally generated from bb.edn (ADR-2607173000 Wave 3); repaired 2026-08-13
;; (ADR-2608131600).  nbb scripts/run-task.cljs <task> [args…]
;;
;; The Wave-3 conversion emitted an EMPTY registry here and deleted bb.edn.
;; `build` was `(clojure.java.shell/sh "bash" "scripts/build.sh")` — a plain
;; two-element argv the converter did not recognise because it matched only the
;; bare symbols `shell` and `clojure`.  A conversion, not a port.
;;
;; Still NOT restored (they need a port, or a runner feature this file cannot
;; express — recovered forms are in scripts/tasks-complex.edn):
;;   test  — a babashka-hosted (clojure.test/run-tests …) body over five ns
;;   serve — needs (shell {:dir "public"} …), and run-task.cljs has no cwd
{:build {:cmd ["bash" "scripts/build.sh"]
         :doc "Write the static CLJC/render-IR authority surface."}}
